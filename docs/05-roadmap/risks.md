# Risk Register — Initial Discovery Risks

| ID | Risk | Evidence | Impact | P00 response / next owner |
|---|---|---|---|---|
| R-001 | Reported legacy scale may conceal stale, duplicate, invalid, spam, or unconsented records. | `CONFIRMED` (`OWNER_INTERVIEW`): the owner reported the count; numerical accuracy and record quality remain unverified. | High | Preserve A-001; P05 authorized audit. |
| R-002 | Full marketplace vision may be treated as a single MVP. | `PROPOSED` broad vision (`TECHNICAL_DISCOVERY`); multiple owner concepts are confirmed in SRC-001, while the detailed owner document is pending. | High | Q-001/Q-002; P01 then P06. |
| R-003 | Multiple countries may introduce unbounded payment, tax, legal, language, support, and location scope. | `CONFIRMED` owner-reported historical reach (`OWNER_INTERVIEW`); multi-country launch is `PROPOSED` (`DAVID_DIRECTIVE`) and launch geography is unknown. | High | Q-003; country scope before architecture commitment. |
| R-004 | Sponsored placement may damage relevance and trust if modeled as a generic featured flag. | `CONFIRMED` promotion concept (`OWNER_INTERVIEW`); rules unvalidated. | High | Preserve organic/sponsored separation; Q-010. |
| R-005 | In-platform communications may face adoption, abuse, consent, retention, and privacy problems. | `PROPOSED` intent (`DAVID_DIRECTIVE`); owner policy and user behavior are unvalidated. | High | Q-008; product/compliance/security discovery. |
| R-006 | External profile acquisition may violate terms, privacy, copyright, or consent expectations. | `PROPOSED` idea (`DAVID_DIRECTIVE`) requiring business ownership and legal/technical validation. | High | No implementation; platform-specific legal/technical review. |
| R-007 | Familiar technologies may be selected before requirements and total cost are understood. | `CONFIRMED` candidate background/preferences (`DAVID_DIRECTIVE`); no decisions. | Medium/High | Technology radar holds all candidates; P03 + ADR gates. |
| R-008 | Six-month discussion may become a perceived promise before scope and team are known. | Discussion is `CONFIRMED` (`OWNER_INTERVIEW`); non-contractual treatment is `CONFIRMED` (`DAVID_DIRECTIVE`). | High | Preserve non-contractual qualifier; P06 ranges/dependencies. |
| R-009 | Pending feature and UX artifacts may contradict current context or each other. | `CONFIRMED` (`OWNER_INTERVIEW`): materials exist but are unavailable. | Medium/High | Source register and post-P01 owner-reconciliation step before product approval. |
| R-010 | Poor taxonomy/geography data may undermine search, SEO, promotions, and migration simultaneously. | `CONFIRMED` owner-reported legacy issues (`OWNER_INTERVIEW`). | High | P01 taxonomy needs + P05 data audit + P02 model. |
| R-011 | Thin or duplicate service/location pages may convert a discovery strategy into search-quality and maintenance debt. | `OBSERVED` competitor pattern + current Google guidance (`EXTERNAL_PRIMARY`); Superola inventory quality is unverified. | High | Gate public pages by governed taxonomy, trustworthy supply, useful content, canonicalization, and locale rules. |
| R-012 | “AI-native” may be misread as a V1 obligation to adopt MCP, A2A, A2UI, UCP, ACP, or `llms.txt`. | `PROPOSED` distribution principle (`TECHNICAL_DISCOVERY`); protocol fit/maturity varies. | High | Preserve truthful human/product semantics; no deliverable whose only V1 consumer is an emerging protocol; architecture mechanisms require later alternatives and ADR/approval gates. |
| R-013 | External agents may expose stale or sensitive data, duplicate RFQs, confuse sponsored/organic results, or execute actions without valid consent. | `PROPOSED` future channel; known protocol/action failure modes (`TECHNICAL_DISCOVERY`). | High | Freshness/provenance, field-level publication, retries that cannot duplicate marketplace effects, authorization, confirmation, auditability, rate controls, and provider opt-out before writes; mechanisms remain later decisions. |
| R-014 | Marketplace Leads may fail one side if direct RFQs produce insufficient customer choice or too little qualified provider demand. | P01 launch slice is `PROPOSED`; Superola user behavior and local liquidity are unvalidated. | High | Preserve A-011/A-013; select bounded cohorts; measure search→request→response→quote→decision by category/geography; reconsider direct vs curated matching only with evidence. |
| R-015 | A universal category model may misrepresent availability, service area, pricing, capacity, travel, or resources. | `PROPOSED` P01 category comparison found materially different candidate semantics across representative archetypes; provider evidence is pending. | High | P02 compares narrow category models, shared-core/extension models, and governed metadata/configuration; no architecture shape or universal boolean/schema is selected in P01. |
| R-016 | Unresponsive or unreachable providers may turn RFQs into a worse experience than a directory. | Legacy quality/reachability is unaudited; notification delivery and response expectations are undefined. | High | Publication/freshness gates, notification-failure state, response measurement, reversible deactivation, honest no-response recovery. |
| R-017 | Subscription may be charged before providers experience attributable value, reducing supply and invalidating the test. | Subscription is an owner-confirmed idea; willingness to pay, package, timing, and liquidity are unvalidated. | High | Free onboarding; value-aware trial gate; segment conversion/retention; owner approval before charging. |
| R-018 | Premature sponsored placement may corrupt organic credibility or create unsellable operational inventory. | Sponsored placement is an owner-confirmed concept; local inventory, demand, rules, and measurement are unvalidated. | High | Stage after credible organic liquidity; separate eligibility/ranking/disclosure/measurement; require provider demand and operating policy. |
| R-019 | Optional AI or realtime behavior may inflate V1 cost without improving the marketplace loop. | AI intent is `PROPOSED`; protocols are future; request/quote flows do not prove a universal realtime requirement. | Medium/High | Deterministic search; separate approved experiment and stop rule; durable observable product state plus separate notification delivery without selecting transport architecture; no protocol-only deliverable. |
| R-020 | Owner ideas may be presented as validated customer/provider demand. | Owner concepts are confirmed as statements, not independently validated market evidence. | High | Preserve qualifiers/provenance; owner meeting and user/provider research; recommendation remains `PROPOSED — OWNER VALIDATION REQUIRED`. |
| R-021 | Marketplace Leads may be approved without quantified TCO, operator capacity, migration/compliance effort, or exit cost. | P01 alternatives are qualitative; no estimates or technology choices exist. | High | Cost A/B0/expanded-B by capability; model fixed/variable spend and staff-hours; compare legacy alternatives; record lock-in/exit and stop/expansion thresholds before scope/budget/schedule approval. |
| R-022 | Customer verification or multiple deliberate one-recipient RFQs may create abandonment, spam, duplicate work, or ambiguous closure. | P01 proposes just-in-time verification and independent RFQs; user behavior is unvalidated. | High | Preserve A-014/Q-017/Q-018; measure abandonment, abuse, simultaneous-open RFQs, and closure behavior; keep recipient sets, auto-routing, and auto-closure out of V1. |

## Added by P02

| ID | Risk | Evidence | Impact | P02 response / next owner |
|---|---|---|---|---|
| R-023 | A deleted or suppressed provider record may be resurrected by a later import pass. | `PROPOSED` (`TECHNICAL_DISCOVERY`): `docs/06-migration/legacy-data-strategy.md` anticipates staged repeated import passes, and deletion without suppression is silently reversible. | High | **Judged the single most likely privacy incident in the plan.** `ADR-009`: a suppression record checked **before** record creation; per-record deletion recorded; batch enumeration and reversal. `Q-024` owns the retention basis. |
| R-024 | The profile-claim flow may become an enumeration oracle over imported third-party contact data. | `PROPOSED` (`TECHNICAL_DISCOVERY`): masked-channel hints in a claim interface disclose record contents before any grant decision. Easy to build by accident. | High | `ADR-009`, `security-privacy-architecture.md` §4: disclose nothing pre-decision; challenge with facts rather than revealing them; failed-attempt counters per record and per actor; operator review on volume. `Q-025`. |
| R-025 | Storing contact data on a request, message, or notification would convert the open contact policy into a system rewrite. | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `A-010` OPEN and `OR-011` unvalidated. A sent notification cannot be recalled. | High | `ADR-010`: contact data is a party attribute behind a recorded disclosure decision; a notification body allowlist. **This is also what keeps `OR-011` off the P02/P04 blocking path.** |
| R-026 | Authorization enforced at the delivery edge would be silently absent in a second channel. | `PROPOSED` (`TECHNICAL_DISCOVERY`): `mvp-scope.md` mandates operator exception paths — a second caller by design — and `R-013` anticipates a future authenticated read path. | High | `ADR-011`: authorization decided in the domain against actor, action, resource, and resource state. Placement, not sophistication. |
| R-027 | A public projection that passes through unknown fields will leak the next private field added. | `PROPOSED` (`TECHNICAL_DISCOVERY`). "We added a field and it leaked" is the standard failure mode of denylist publication. | High | `ADR-012`: allowlist-derived, versioned, rebuildable projection; unknown fields absent; search read path fed only through the publication policy. |
| R-028 | Legacy and new-platform coexistence may split provider identity and forfeit accumulated search authority. | `ASSUMPTION` (`A-019`): coexistence is new in the working envelope; no P01 position and no analytics baseline (`SRC-006` NOT RECEIVED). | High | `ADR-009`: no runtime coupling in either direction; coexistence is edge routing. `Q-019` must settle identity ownership, canonical URL ownership, redirect authority, and the cutover trigger before any cutover mechanism is chosen. |
| R-029 | "Future-proofing is free" may inflate Phase 1 with seams justified only by deferred scope. | `ASSUMPTION` (`A-020`) over the anti-inflation rule in `docs/07-research/ai-discoverability.md` and `AGENTS.md`. | Medium/High | `system-architecture.md` §6 admissibility test: a future-motivated boundary enters P02 only if independently justified by a Phase-1 need, with that justification written beside it. `Q-022` measures the deferral cost. |
| R-030 | Partitioning accounts by marketplace role would make the routine dual-role case a migration. | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `CONFIRMED` owner-reported overlapping performer categories. | Medium/High | `ADR-004`: an account is not typed by role. `Q-028` needs only ratification — David can settle it without owner input. |
| R-031 | A numeric non-functional-requirements table, or sizing derived from the unaudited legacy count, would silently commit infrastructure spend. | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `A-001` and `R-001`: the ~43,000 count is owner-reported and unaudited, and `SRC-006` is NOT RECEIVED. | High | P02 published **no** numeric non-functional table, deliberately, and states which commitment each kind of number buys. `p03-decision-inputs.md` forbids any number without a source and a volume scenario. |
| R-032 | Unenforced module boundaries would turn the recommended architecture into a monolith with folders, paying monolith exit costs while having advertised modularity. | `PROPOSED` (`TECHNICAL_DISCOVERY`): nothing physically prevents a cross-module join, and deadline pressure attacks boundaries first. | High | `ADR-001` and `D-10`: **mechanical boundary enforcement in the build is inseparable from the decision** — a forbidden cross-module import must fail the build. Without it, `ADR-001` is claimed rather than implemented. |

## Added by P03

| ID | Risk | Evidence | Impact | P03 response / next owner |
|---|---|---|---|---|
| R-033 | **The public-acquisition hypothesis carries two unbounded variable-cost lines, not one, and no document models them together.** | `PROPOSED` (`TECHNICAL_DISCOVERY`). `D-06` names served bytes per anonymous public view as *"the one cost that grows with acquisition success rather than with revenue-bearing requests"*; `D-13` says the same of per-query intent-mapper cost. **Each is written as if it were the sole exception.** | High | `docs/03-technology/cost-model.md` models both jointly against the same traffic assumption. `docs/03-technology/ai-evaluation.md` §6 recommends against funding the mapper at all, which removes one of the two. `D-06`'s link-out alternative would materially reduce the other. |
| R-034 | **No import-graph enforcement tool in any candidate stack can see a cross-module foreign key or a cross-module table reference inside a SQL string — which is the half of `ADR-001` that actually determines exit cost.** | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `ADR-001`'s own statement that *"the shared record store is the real trap, not the shared process"*. ArchUnit documents itself as bytecode-only; build-tool dependency configurations see types, not strings. | High | Per-module database schemas plus a build-failing integration test asserting **no foreign key crosses a schema boundary**, queried from the catalog. Optional trigger-gated escalation: per-module database roles, which make a cross-module join fail at runtime rather than merely being forbidden. **Without this, `R-032`'s mitigation is only half implemented in every candidate stack.** |
| R-035 | **`robots.txt` is not an enforcement mechanism, and P02's "ONE enforcement point" language implies that it is.** | `OBSERVED` (`EXTERNAL_PRIMARY`): RFC 9309 states *"These rules are not a form of access authorization"* and *"The Robots Exclusion Protocol is not a substitute for valid content security measures."* Compliance is voluntary. | Medium/High | Restate as **one governed policy, two artifacts**: a request-path filter that decides allow or deny before rendering (enforcement, and even then best-effort against a non-cooperating client), and a published `robots.txt` **derived** from the same governed data, never authored separately. `ADR-012`'s deny-by-default claim holds for the decision point, not against a determined crawler. |
| R-036 | **Framework support cadence may become a material recurring cost for a one-person team, and it is usually discovered rather than budgeted.** | `OBSERVED` (`EXTERNAL_PRIMARY`): the recommended framework line carries a ~12-month open-source support window per minor release with a roughly six-month release cadence; one candidate platform has two consecutive versions expiring on the same date. | Medium | Recorded explicitly in the technology ADRs and in the human-cost line of `docs/03-technology/cost-model.md` rather than left implicit. Reconsideration trigger: measured upgrade effort exceeding the budgeted annual allowance for two consecutive cycles. |
| R-037 | **Enforcing module data ownership with per-module database roles would forbid the cross-module reads `system-architecture.md` §4 explicitly requires**, making the request-creation transaction impossible to express. | `PROPOSED` (`TECHNICAL_DISCOVERY`): §4 requires request creation to include an *"eligibility re-check against **Provider source truth**"* inside **one transaction in Demand**. A role granted `SELECT` only on its own schema cannot perform that read. | Medium/High | **The mechanism is the problem, not `ADR-001`.** `ADR-001` forbids cross-module **joins** and **referential constraints**; it does not forbid a module reading another module's truth through that module's interface, and `domain-map.md` already places composition in the application layer. Resolution adopted: schema-per-module for namespacing and grants; cross-module `SELECT` permitted; **writes and referential constraints module-scoped**; joins prevented in the build (`R-032`) and FK-crossing prevented by the catalog test (`R-034`). Per-module write roles remain available as a trigger-gated escalation. |
| R-038 | **"Deletion proof" is recorded as a lock-in and exit criterion that no database product can satisfy, so it will be silently marked satisfied by any store with a bulk-export command.** | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `docs/05-roadmap/mvp-scope.md`'s lock-in row. | Medium/High | Restate as a **product capability**, not a store feature: a per-subject enumeration query complete by construction (which requires `security-privacy-architecture.md` §12's classification to be a machine-readable registry rather than a document), an audit record written by a **different role** recording request, scope and completion, a re-run returning zero retained as evidence, and an **explicit disclosed exclusion for the backup and point-in-time-recovery window**. PITR that can restore to before a deletion is a truthful limit and must be stated, not hidden. **No technology record may report "deletion proof: yes" against a store.** |
| R-039 | **A single `timestamptz` column silently fails data-architecture rule 6, and the failure is unrecoverable.** | `PROPOSED` (`TECHNICAL_DISCOVERY`): rule 6 requires *"every instant carries the zone it belongs to — and for event dates, that zone is the **service location's**"*. A `timestamptz` stores an instant, not the zone it belongs to. | Medium/High | Two columns, from the first migration: the instant, plus the IANA zone identifier resolved from the service location. `internationalization-architecture.md` §3 already rates retrofitting this **"HIGH and often unrecoverable"** — ambiguous historical wall-clock values cannot be reconstructed. Validation: a schema test asserting every event-date column has a paired zone column. |
| R-040 | **Passwordless email-only authentication would silently cancel `D-05`'s free cost lever.** | `PROPOSED` (`TECHNICAL_DISCOVERY`). `D-05` names the free lever: make in-app request state authoritative and complete, so notification failure degrades **speed, not correctness**. That lever requires users to reach in-app state **without email**. If authentication is magic-link or email-OTP only, an email outage locks users out of the very state email failure was supposed to survive. **Neither `D-05` nor `D-08` notices the interaction.** | High | **Superola must retain at least one non-email credential path.** Recorded as a hard constraint in `D-08`. It also qualifies `R-022`'s friction-minimisation instinct: the lowest-friction mechanism concentrates all access on the single channel the architecture already treats as unreliable. |
| R-041 | **Vendor-side email suppression is global per address, so a marketing complaint can suppress transactional mail — inside the vendor, where Superola's purpose-class model has no reach.** | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `security-privacy-architecture.md` §5, which designs separable consent purpose classes but names no vendor-side mechanism. | Medium/High | **Separate provider configuration sets and separate sending subdomains per purpose class from day one**, so reputation and suppression are physically partitioned. Cheap now; impossible to retrofit after reputation damage. Also: no P02 document asks **how long a vendor retains the message body** — and the body carries a non-guessable link into the authenticated surface, extending §12's "permanently exfiltrated" into a second system with its own breach clock. |
| R-042 | **`PublicationRequirement`'s "required media roles" risks conflating media *existence* with media *servability*, so a delivery-network outage could silently unpublish supply.** | `PROPOSED` (`TECHNICAL_DISCOVERY`). "This profile has media" is a database fact; "the delivery network can currently serve it" is a vendor fact. | Medium | Publication gates on the database fact only. A media-delivery outage degrades public pages to placeholders and must never alter publication state or discovery eligibility. Validation: unplug the delivery network in a test and assert published supply is unchanged. |
| R-043 | **Hosting performer audio and video would import music-licensing exposure that appears nowhere in the cost model, in exactly the owner-reported launch categories.** | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `CONFIRMED` owner-reported categories (mariachi, norteño groups, banda, DJs). When Superola hosts, Superola makes the copy and publishes it, and `MediaRightsDeclaration` is the only thing between Superola and a claim — **but the performance may be the provider's while the composition almost never is.** | High | `D-06`'s link-out alternative for audio and video. Embedding leaves the copy on the host platform, where its own rights adjudication has already run, and Superola is a linker. **Link-out does not weaken the rights position; it materially strengthens it, in precisely the category where audio and video are central.** Also removes ~15× per-asset human moderation cost and the largest variable infrastructure line in the product. |
| R-044 | **`system-architecture.md` §4's rule that the notification delivery intent is created *after* the business transaction commits — "never in the same transaction, ever" — creates a silent lost-notification window once the job store is the same database.** | `PROPOSED` (`TECHNICAL_DISCOVERY`). The rule is correct for a **broker**, because you cannot atomically commit to two systems. With a same-database job table it produces the failure it was written to prevent: a crash between the business commit and the intent insert leaves the request durable, the provider never notified, **and no dead-letter entry, because no entry was ever created.** `R-016` and P16 require delivery failure and human non-response never to collapse — this failure mode produces **neither state. It produces silence.** §3 already states the requirement correctly as *"best-effort delivery over a **durable intent**"*, and an intent that can be lost is not durable. | **High** | **§4 corrected in place: the prohibition is re-scoped from the intent to the external call.** The durable intent commits **with** the business transaction; the side effect — the external send — executes after commit, in a separate process, in its own transaction per attempt. This is the **transactional outbox**. *(Adversarial review corrected an overreach here: an earlier draft claimed a same-store queue was the **only** mechanism satisfying the requirement. It is not — a broker fronted by an outbox satisfies it equally, because the outbox is precisely how a broker avoids the dual-write problem. The correct argument is that **the outbox table is required either way, so the broker is strictly additive** at 0.096 jobs/second. `ADR-015`.)* |
| R-045 | **Resolving customer location through a geocoding vendor on the search path would scale the geocoding bill with search volume, the highest-volume lowest-intent action in the product.** | `PROPOSED` (`TECHNICAL_DISCOVERY`). At the Growth assumption of 600,000 searches/month against a verified permanent-geocoding rate with no free tier, the geocoding line would exceed the entire rest of the infrastructure bill several times over. | High | **State the rule explicitly rather than leave it implied: geocode provider base addresses and unresolved free text only — never per search.** Customer-side location resolves against the governed internal `Place` list, which `domain-model.md` §1.4 already specifies. This is the same unit-economics inversion `D-13` warns about, appearing in a second capability, and no P02 document named it. |
| R-046 | **A delivery-network bot-mitigation product is a second, vendor-owned machine-access enforcement point that can silently contradict the approved crawler policy.** | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `ADR-012`, which requires machine access to be a per-surface per-crawler-class switch defaulting to deny with **one enforcement point and one owner**, and warns that bot mitigation must not silently contradict it. Entry-tier bot products challenge automated traffic on the vendor's own heuristics, invisible to the governed policy record. | Medium/High | Do not enable heuristic bot mitigation while the crawler gate is closed. Keep **one governed policy record**; **generate both `robots.txt` and the edge rules from it**; hand-edit neither; and **fail the deploy if the two renderings diverge**. If mitigation is later needed alongside an approved crawler, the minimum viable tier is the one supporting rule-level exceptions — bought to satisfy the ADR, not for performance. |
| R-047 | **"A fast pipeline so rollback is cheap" is true for code and false for schema, and P02 does not say which half.** | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `D-10`. On a single deployable with rolling zero-downtime deploys, **old and new code run simultaneously against one schema.** A rollback after a destructive migration is not a rollback — it is a restore, with data loss between the migration and the restore point. | Medium/High | Three rules, all free to adopt and expensive to retrofit: (1) **every migration is expand/contract**; (2) **no destructive DDL in the same release as the code change that stops using the column — minimum one release gap**; (3) **migrations run as a pre-deploy step, never on application startup**, because concurrent replicas race the migration lock. Add: every migration is tested against the **previous** release's code, or expand/contract is a convention rather than a guarantee. |
| R-048 | **Provider media EXIF metadata routinely carries precise GPS coordinates, so publishing a photo emits to the public projection the exact field `security-privacy-architecture.md` §12 classifies provider-private.** | `PROPOSED` (`TECHNICAL_DISCOVERY`). §12 states precise provider base location is *"readable by the eligibility computation, never emitted to a projection, a search result, a notification, or analytics."* A published photograph with intact EXIF emits it to the projection. **No P02 document names this path.** | High | **Strip all metadata during the derivative re-encode that `D-06` already requires.** Cost: one flag on an operation that must happen anyway. Validation: a test asserting no derivative carries EXIF GPS tags. Note this is a *free* fix only because the fixed-derivative-set decision already forces a re-encode — under an on-the-fly transformation model it would be a separate control. |
| R-049 | **Analytics segmentation fine enough to be useful can be fine enough to re-identify at launch volumes.** | `PROPOSED` (`TECHNICAL_DISCOVERY`). §12 requires analytics events to be pseudonymous with no free text and no contact data; `D-11` requires segmentation by `market`, `locale` and `archetype` from day one. Compatible — but the next dimension anyone adds is fine-grained geography, and at ~150 published providers a (market, locale, archetype, place) tuple can be uniquely identifying. | Medium | State as a rule in the analytics record shape: **place is carried at a coarse governed level only.** The mandatory segmentation dimensions are sufficient for the funnel; finer geography is a separate approval with a re-identification assessment, not a default. |

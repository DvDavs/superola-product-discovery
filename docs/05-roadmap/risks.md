# Risk Register — Initial Discovery Risks

| ID | Risk | Evidence | Impact | P00 response / next owner |
|---|---|---|---|---|
| R-001 | Reported legacy scale may conceal stale, duplicate, invalid, spam, or unconsented records. | `CONFIRMED` (`OWNER_INTERVIEW`): the owner reported the count; numerical accuracy and record quality remain unverified. | High | Preserve A-001; P05 authorized audit. |
| R-002 | Full marketplace vision may be treated as a single MVP. | `PROPOSED` broad vision (`TECHNICAL_DISCOVERY`); multiple owner concepts are confirmed in SRC-001, while the detailed owner document is pending. | High | Q-001/Q-002; P01 then P06. |
| R-003 | Multiple countries may introduce unbounded payment, tax, legal, language, support, and location scope. | `CONFIRMED` owner-reported historical reach (`OWNER_INTERVIEW`); multi-country launch is `PROPOSED` (`DAVID_DIRECTIVE`) and launch geography is unknown. | High | Q-003; country scope before architecture commitment. |
| R-004 | Sponsored placement may damage relevance and trust if modeled as a generic featured flag. | `CONFIRMED` promotion concept (`OWNER_INTERVIEW`); rules unvalidated. | High | Preserve organic/sponsored separation; Q-010. |
| R-005 | In-platform communications may face adoption, abuse, consent, retention, and privacy problems. | `PROPOSED` intent (`DAVID_DIRECTIVE`); owner policy and user behavior are unvalidated. | High | Q-008; product/compliance/security discovery. |
| R-006 | External profile acquisition may violate terms, privacy, copyright, or consent expectations. | `PROPOSED` idea (`DAVID_DIRECTIVE`) requiring business ownership and legal/technical validation. | High | No implementation; platform-specific legal/technical review. |
| R-007 | Familiar technologies may be selected before requirements and total cost are understood. | `CONFIRMED` candidate background/preferences (`DAVID_DIRECTIVE`); no decisions. | Medium/High | Technology radar holds all candidates; P03 + ADR gates. |
| R-008 | Six-month discussion may become a perceived promise before scope and team are known. | Discussion is `CONFIRMED` (`OWNER_INTERVIEW`); non-contractual treatment is `CONFIRMED` (`DAVID_DIRECTIVE`). | High | Preserve non-contractual qualifier; P06 ranges/dependencies. |
| R-009 | Pending feature and UX artifacts may contradict current context or each other. | `CONFIRMED` (`OWNER_INTERVIEW`): materials exist but are unavailable. | Medium/High | Source register and post-P01 owner-reconciliation step before product approval. |
| R-010 | Poor taxonomy/geography data may undermine search, SEO, promotions, and migration simultaneously. | `CONFIRMED` owner-reported legacy issues (`OWNER_INTERVIEW`). | High | P01 taxonomy needs + P05 data audit + P02 model. |
| R-011 | Thin or duplicate service/location pages may convert a discovery strategy into search-quality and maintenance debt. | `OBSERVED` competitor pattern + current Google guidance (`EXTERNAL_PRIMARY`); Superola inventory quality is unverified. | High | Gate public pages by governed taxonomy, trustworthy supply, useful content, canonicalization, and locale rules. |
| R-012 | “AI-native” may be misread as a V1 obligation to adopt MCP, A2A, A2UI, UCP, ACP, or `llms.txt`. | `PROPOSED` distribution principle (`TECHNICAL_DISCOVERY`); protocol fit/maturity varies. | High | Preserve truthful human/product semantics; no deliverable whose only V1 consumer is an emerging protocol; architecture mechanisms require later alternatives and ADR/approval gates. |
| R-013 | External agents may expose stale or sensitive data, duplicate RFQs, confuse sponsored/organic results, or execute actions without valid consent. | `PROPOSED` future channel; known protocol/action failure modes (`TECHNICAL_DISCOVERY`). | High | Freshness/provenance, field-level publication, retries that cannot duplicate marketplace effects, authorization, confirmation, auditability, rate controls, and provider opt-out before writes; mechanisms remain later decisions. |
| R-014 | Marketplace Leads may fail one side if direct RFQs produce insufficient customer choice or too little qualified provider demand. | P01 launch slice is `PROPOSED`; Superola user behavior and local liquidity are unvalidated. | High | Preserve A-011/A-013; select bounded cohorts; measure search→request→response→quote→decision by category/geography; reconsider direct vs curated matching only with evidence. |
| R-015 | A universal category model may misrepresent availability, service area, pricing, capacity, travel, or resources. | `PROPOSED` P01 category comparison found materially different candidate semantics across representative archetypes; provider evidence is pending. | High | P02 compares narrow category models, shared-core/extension models, and governed metadata/configuration; no architecture shape or universal boolean/schema is selected in P01. |
| R-016 | Unresponsive or unreachable providers may turn RFQs into a worse experience than a directory. | Legacy quality/reachability is unaudited; notification delivery and response expectations are undefined. | High | Publication/freshness gates, notification-failure state, response measurement, reversible deactivation, honest no-response recovery. |
| R-017 | Subscription may be charged before providers experience attributable value, reducing supply and invalidating the test. | Subscription is an owner-confirmed idea; willingness to pay, package, timing, and liquidity are unvalidated. | High | Free onboarding; value-aware trial gate; segment conversion/retention; owner approval before charging. |
| R-018 | Premature sponsored placement may corrupt organic credibility or create unsellable operational inventory. | Sponsored placement is an owner-confirmed concept; local inventory, demand, rules, and measurement are unvalidated. | High | Stage after credible organic liquidity; separate eligibility/ranking/disclosure/measurement; require provider demand and operating policy. |
| R-019 | Optional AI or realtime behavior may inflate V1 cost without improving the marketplace loop. | AI intent is `PROPOSED`; protocols are future; request/quote flows do not prove a universal realtime requirement. | Medium/High | Deterministic search; separate approved experiment and stop rule; durable observable product state plus separate notification delivery without selecting transport architecture; no protocol-only deliverable. |
| R-020 | Owner ideas may be presented as validated customer/provider demand. | Owner concepts are confirmed as statements, not independently validated market evidence. | High | Preserve qualifiers/provenance; owner meeting and user/provider research; recommendation remains `PROPOSED — OWNER VALIDATION REQUIRED`. |
| R-021 | Marketplace Leads may be approved without quantified TCO, operator capacity, migration/compliance effort, or exit cost. | P01 alternatives are qualitative; no estimates or technology choices exist. | High | Cost A/B0/expanded-B by capability; model fixed/variable spend and staff-hours; compare legacy alternatives; record lock-in/exit and stop/expansion thresholds before scope/budget/schedule approval. |
| R-022 | Customer verification or multiple deliberate one-recipient RFQs may create abandonment, spam, duplicate work, or ambiguous closure. | P01 proposes just-in-time verification and independent RFQs; user behavior is unvalidated. | High | Preserve A-014/Q-017/Q-018; measure abandonment, abuse, simultaneous-open RFQs, and closure behavior; keep recipient sets, auto-routing, and auto-closure out of V1. |

## Added by P02

| ID | Risk | Evidence | Impact | P02 response / next owner |
|---|---|---|---|---|
| R-023 | A deleted or suppressed provider record may be resurrected by a later import pass. | `PROPOSED` (`TECHNICAL_DISCOVERY`): `docs/06-migration/legacy-data-strategy.md` anticipates staged repeated import passes, and deletion without suppression is silently reversible. | High | **Judged the single most likely privacy incident in the plan.** `ADR-009`: a suppression record checked **before** record creation; per-record deletion recorded; batch enumeration and reversal. `Q-024` owns the retention basis. |
| R-024 | The profile-claim flow may become an enumeration oracle over imported third-party contact data. | `PROPOSED` (`TECHNICAL_DISCOVERY`): masked-channel hints in a claim interface disclose record contents before any grant decision. Easy to build by accident. | High | `ADR-009`, `security-privacy-architecture.md` §4: disclose nothing pre-decision; challenge with facts rather than revealing them; failed-attempt counters per record and per actor; operator review on volume. `Q-025`. |
| R-025 | Storing contact data on a request, message, or notification would convert the open contact policy into a system rewrite. | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `A-010` OPEN and `OR-011` unvalidated. A sent notification cannot be recalled. | High | `ADR-010`: contact data is a party attribute behind a recorded disclosure decision; a notification body allowlist. **This is also what keeps `OR-011` off the P02/P04 blocking path.** |
| R-026 | Authorization enforced at the delivery edge would be silently absent in a second channel. | `PROPOSED` (`TECHNICAL_DISCOVERY`): `mvp-scope.md` mandates operator exception paths — a second caller by design — and `R-013` anticipates a future authenticated read path. | High | `ADR-011`: authorization decided in the domain against actor, action, resource, and resource state. Placement, not sophistication. |
| R-027 | A public projection that passes through unknown fields will leak the next private field added. | `PROPOSED` (`TECHNICAL_DISCOVERY`). "We added a field and it leaked" is the standard failure mode of denylist publication. | High | `ADR-012`: allowlist-derived, versioned, rebuildable projection; unknown fields absent; search read path fed only through the publication policy. |
| R-028 | Legacy and new-platform coexistence may split provider identity and forfeit accumulated search authority. | `ASSUMPTION` (`A-019`): coexistence is new in the working envelope; no P01 position and no analytics baseline (`SRC-006` NOT RECEIVED). | High | `ADR-009`: no runtime coupling in either direction; coexistence is edge routing. `Q-019` must settle identity ownership, canonical URL ownership, redirect authority, and the cutover trigger before any cutover mechanism is chosen. |
| R-029 | "Future-proofing is free" may inflate Phase 1 with seams justified only by deferred scope. | `ASSUMPTION` (`A-020`) over the anti-inflation rule in `docs/07-research/ai-discoverability.md` and `AGENTS.md`. | Medium/High | `system-architecture.md` §6 admissibility test: a future-motivated boundary enters P02 only if independently justified by a Phase-1 need, with that justification written beside it. `Q-022` measures the deferral cost. |
| R-030 | Partitioning accounts by marketplace role would make the routine dual-role case a migration. | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `CONFIRMED` owner-reported overlapping performer categories. | Medium/High | `ADR-004`: an account is not typed by role. `Q-028` needs only ratification — David can settle it without owner input. |
| R-031 | A numeric non-functional-requirements table, or sizing derived from the unaudited legacy count, would silently commit infrastructure spend. | `PROPOSED` (`TECHNICAL_DISCOVERY`) over `A-001` and `R-001`: the ~43,000 count is owner-reported and unaudited, and `SRC-006` is NOT RECEIVED. | High | P02 published **no** numeric non-functional table, deliberately, and states which commitment each kind of number buys. `p03-decision-inputs.md` forbids any number without a source and a volume scenario. |
| R-032 | Unenforced module boundaries would turn the recommended architecture into a monolith with folders, paying monolith exit costs while having advertised modularity. | `PROPOSED` (`TECHNICAL_DISCOVERY`): nothing physically prevents a cross-module join, and deadline pressure attacks boundaries first. | High | `ADR-001` and `D-10`: **mechanical boundary enforcement in the build is inseparable from the decision** — a forbidden cross-module import must fail the build. Without it, `ADR-001` is claimed rather than implemented. |

# P03.1 — Technology Decision Reconciliation & Cost Explainability

## Metadata

- ID: P03.1
- Title: Technology Decision Reconciliation & Cost Explainability
- Owner: David
- Status: COMPLETED
- Authorization: explicit David authorization for a **bounded follow-up to a completed P03**. Not a new research phase.
- Baseline: P03 complete at commit `3e10d14`. P02 complete.
- Technology status produced by this phase: unchanged — `PROPOSED`. **This phase approved nothing.**

## Objective

Reconcile P03's proposed decisions, correct decision **packaging**, improve cost **explainability**, and prepare a clean handoff to P04.

Converge quickly. **Do not re-run P03.**

## Non-goals

- Re-evaluating the stack. PostgreSQL, Spring, hosting, media, email, search, queues and observability were **not** reopened; no concrete material defect was found in them.
- Broad research. Maximum advisory work was **one** bounded authentication verification and **one** final adversarial review.
- Changing any cost arithmetic. The three scenario totals are identical to those committed in P03.
- Reversing the P03 AI recommendation.
- Building the P07 HTML artifact.
- Starting P04, P05, P06, or P07.
- Pushing, creating or modifying remotes, or touching historical stashes.

## Context and authoritative inputs

| Input | Why it is authoritative here |
|---|---|
| `docs/02-architecture/adr/ADR-013`–`ADR-019` | The decisions being reconciled. |
| `docs/02-architecture/adr/README.md` | **Defines `ACCEPTED` as approved by the human decision owner.** This is what determines whether `ADR-013` may change status. |
| `docs/03-technology/cost-model.md` | The single arithmetic source of truth. Not recomputed by this phase. |
| `docs/03-technology/ai-evaluation.md` | §8, §10 — the recommendation this phase must not reverse. |
| `plans/P03-technology-evaluation.md` | The gap register, the specialist-coverage note, and the three cheap verifications owed before purchase. |
| `AGENTS.md` | Evidence labels, approval discipline, and the new prefix/currency/cost-framing policies added by this phase. |

## Known constraints and evidence labels

- **Nothing in P03 is adopted.** Every ADR is `PROPOSED`; only `ADR-004` is `ACCEPTED`, on an explicit `DAVID_DIRECTIVE` naming it.
- **No traffic evidence exists.** `SRC-006` NOT RECEIVED. Every volume is an `ASSUMPTION`.
- `G-02`, `G-06`, `G-09` remain unresolved. `G-06` still blocks P04.
- The P01.1 release gate remains unsatisfied.

## Tasks and delegation strategy

The primary agent is the canonical writer. Two advisory read-only agents only, exactly as bounded.

- [x] Add a repository-wide **stable document prefix policy**, a **currency** policy (USD baseline), and a **cost framing** policy to `AGENTS.md`; reconcile `PLANS.md`'s conflicting header requirement.
- [x] Run **one** bounded authentication verification (Supabase, Auth0, Clerk, Cognito; six questions; official sources only) and persist the primary-source evidence.
- [x] Write `docs/03-technology/p03-decision-reconciliation.md` with a per-ADR disposition table.
- [x] **Split `ADR-013`**: refocus on platform only; create `ADR-020` for rendering as `PROPOSED — DEFERRED, PENDING P04 UX VALIDATION`.
- [x] **Narrow `ADR-019`** into three decision levels: architecture (`RECOMMEND ACCEPT`), vendor (`HOLD — LEGAL`), rendered map (`MOVED TO P04`).
- [x] Update `ADR-017` to `HOLD` and correct its exit argument against the verification result.
- [x] Write `docs/03-technology/cost-alternatives.md` — why each cost decision was taken and what would flip it.
- [x] Reframe `cost-model.md` §1: cash bill primary, human cost a separately labelled illustrative sensitivity; USD explicit.
- [x] Add the bounded AI-assisted intake experiment to `ai-evaluation.md` §8.1 without reversing §10.
- [x] Write bilingual `presentation/cost-explainer-v0.1.md`.
- [x] Update `presentation/technology-preview-v0.1.md`.
- [x] Persist the P07 HTML and Excalidraw requirements in `presentation/outline.md`.
- [x] Update `adr/README.md`, `plans/README.md`, `README.md`, `CHANGELOG.md`.
- [x] Run one adversarial review; resolve CRITICAL and HIGH only.
- [x] Validate; run `git diff --check`; commit. **Do not push.**

## Decisions made or ADRs required

**No approvals.** The dispositions are recommendations, recorded in `docs/03-technology/p03-decision-reconciliation.md`:

| ADR | Disposition |
|---|---|
| `ADR-013` platform | `RECOMMEND ACCEPT` — status stays `PROPOSED` |
| `ADR-014` PostgreSQL | `RECOMMEND ACCEPT` |
| `ADR-015` outbox | `RECOMMEND ACCEPT` |
| `ADR-016` managed platform | `RECOMMEND ACCEPT` — region still deferred |
| `ADR-017` authentication | **`HOLD`** — its own reconsideration trigger fired |
| `ADR-018` media | `RECOMMEND ACCEPT` |
| `ADR-019` Level 1 governed `Place` | `RECOMMEND ACCEPT` |
| `ADR-019` Level 2 vendor | `HOLD — LEGAL/TERMS VERIFICATION REQUIRED` |
| `ADR-019` Level 3 map | `MOVE PRODUCT/UX PART TO P04` |
| `ADR-020` rendering (new) | `SPLIT` → `HOLD / DEFERRED PENDING P04` |

**One ADR added: `ADR-020`.** It records that no rendering decision has been made, which is a decision about decision scope rather than a technology selection — and it exists because leaving rendering inside `ADR-013` would have made silence look like approval.

### The finding that changed a P03 conclusion

**P03's most important unverified item is now verified, and the P03 claim was falsified for two of four vendors.** Password-hash export is **self-service and documented** for Supabase Auth (bcrypt in `auth.users.encrypted_password`) and Clerk (dashboard CSV *"includes their hashed passwords"*); **gated behind a support case** for Auth0; **no documented mechanism** for Cognito, where an explicit prohibition remains NOT VERIFIED.

`ADR-017` therefore moves to `HOLD`. **It is not reversed** — the objections that survive are revocation semantics, the `VerificationFact` model, and `ADR-004` dual-role account shape, and Option A still satisfies all of them. **What is withdrawn is the claim that decided it.**

Evidence: `docs/07-research/authentication-vendor-verification.md`, all sources accessed 2026-08-12.

## Open questions and approval gates

All prior gates unchanged. P03.1 adds no new question IDs and resolves none.

**New items owed to a named owner:**

1. **Clerk's exported hash format** — one export from a test instance. Owner: David.
2. **Is *immediate* revocation a hard requirement?** A policy question, not a research question. Owner: David. **If a bounded window is acceptable the authentication field reopens.**
3. **Auth0 support-case export SLA** — only if Auth0 remains a candidate.
4. **The two geocoding legal readings** — counsel. Unchanged from P03, still blocking `ADR-019` Level 2.
5. **The object-storage custom-domain content-type seam** — unchanged from P03, still the highest-priority check before `ADR-018`.

**The `ADR-013` governance question is answered, not open:** the ADR index defines `ACCEPTED` as approved by the human decision owner, the only precedent required an explicit `DAVID_DIRECTIVE` naming the ADR, and `SRC-014` authorized evaluation rather than approval. **Explicit individual approval is required, so `ADR-013` stays `PROPOSED`.**

## Validation and acceptance criteria

| Check | Result |
|---|---|
| No blanket approval implied anywhere. | **PASS.** `p03-decision-reconciliation.md` states it in its own header, §3 shows the governance reasoning, and no ADR status changed to `ACCEPTED`. |
| Infrastructure cash and human cost never summed in a headline. | **PASS.** `cost-model.md` §1 split into §1.1 and §1.2; the preview's combined-total column removed; the explainer keeps them in separate sections with an explicit "do not add these" instruction. |
| Cost arithmetic does not diverge. | **PASS.** No total was recomputed. `cost-alternatives.md` reproduces `cost-model.md` §1.1 and declares it the single source of truth. Category anatomies sum to USD $75.98 / $192.99 / $560.79. |
| Excluded costs remain visible. | **PASS.** Payments, legacy migration, marketing/acquisition/legal, salaries and a rendered map are listed in `cost-alternatives.md` §11 and the explainer §6. |
| Frontend rendering not locked before P04. | **PASS.** `ADR-020` created as `HOLD/DEFERRED`; `ADR-013` scoped to platform; the preview marks rendering as a starting point, not a decision. |
| Unverified auth export not treated as impossible. | **PASS — and it was verified rather than assumed either way.** The blanket "may be impossible" claim is withdrawn; Cognito is recorded as *no documented mechanism*, explicitly not *proven impossible*. |
| Legal geocoder interpretation not stated as technical fact. | **PASS.** `ADR-019` Level 2 is `HOLD — LEGAL`, and both readings are labelled unresolved interpretations requiring counsel. |
| Map/privacy distinction correct. | **PASS.** The privacy invariant is retained absolutely; the product conclusion is moved to P04 with a named re-price trigger. |
| AI wording neither reversed nor inflated. | **PASS.** §10 unchanged. §8.1 added as a bounded, optional, capped, P04-owned experiment with the privacy gate (`Q-033`) and the guided-form prerequisite both binding. |
| No documentation churn caused only by timestamps. | **PASS.** No `Last updated` header was refreshed. Volatile dates moved to footers on documents this phase substantively modified; **no repository-wide metadata sweep was performed**, per the policy's own instruction. |
| `git diff --check` passes. | **PASS.** |
| Adversarial review resolved at CRITICAL/HIGH. | **PASS on the second pass. The first returned FAIL: three CRITICAL and two HIGH.** All resolved; findings recorded below rather than summarised away. |

### Adversarial review findings and their resolution

**The first pass returned FAIL.** Two of the three CRITICAL findings were arithmetic errors in **new P03.1 text**, in a document whose own header claims every figure reconciles — **the exact failure mode this phase was created to prevent, committed by this phase.** They are recorded rather than quietly fixed.

| Finding | Severity | Resolution |
|---|---|---|
| **A media-share percentage that did not reproduce.** New text claimed that at USD $0.15/GB *"the media line would be 67% of the entire modelled bill"* — dividing a hypothetical USD $375 line by USD $560.79, **a total that excludes it**, while the paired 0.6% used the correct denominator | **CRITICAL** | Recomputed: the bill would become USD $932.26 and media would be **40%** of it — still the largest single line, ahead of the database. **The corrected figure makes a weaker-sounding but true claim, and the true one is sufficient** |
| **A multiple republished without checking it.** *"USD $6,000 … 4.6× the entire verified infrastructure bill at Growth"* — 6,000 ÷ 560.79 = **10.7×**, and 4.6× reproduces from no denominator in the model. Inherited from P03 and **carried unchanged into a new document asserting reconciliation** | **CRITICAL** | Corrected to **10.7×** in `cost-alternatives.md` §9 and `cost-model.md` §8, with the correction stated in place |
| **A false completeness claim.** The `CHANGELOG` asserted the combined total-cost-of-ownership figure was *"no longer presented as a headline anywhere"*, while `technology-evaluation.md` §1.2 was still **titled** *"Human cost is 88–96% of total cost of ownership"* and both §1.2 and §5 carried a `Total cost of ownership \| ~$654 \| ~$2,784 \| ~$14,859` row — **directly contradicting `cost-model.md` §1's new statement that the model does not support that claim** | **CRITICAL** | `technology-evaluation.md` §1.2 and §5 split the same way `cost-model.md` §1 was, with the human rows labelled a sensitivity and marked *not added above*. **The claim was corrected by making it true rather than by narrowing it** |
| **`technology-radar.md` was not reconciled for `ADR-019`.** The geocoder still read `SELECTED` while `ADR-019` Level 2 says `HOLD — LEGAL` and *"no purchase may proceed"*; the map row still read `REJECTED for V1` with the conflated invariant-plus-conclusion reasoning P03.1 says it corrected. `README.md` designates the radar *"the one place vendor names carry a recommendation"* | **HIGH** | Geocoder row now reads `RECOMMENDED, but HOLD — LEGAL/TERMS VERIFICATION REQUIRED`; map row now reads `MOVED TO P04 — not rejected`, with the invariant stated separately from the assumption |
| **Two contradictory Clerk rows in one radar table** — `NEW IN THE COMPARISON` and `REJECTED`, neither referencing the other, in a table whose ring column is the verdict | **HIGH** | Merged into one row: `REJECTED in P03 — REOPENED by P03.1 pending the ADR-017 re-run`, carrying both the P03 objection and the P03.1 findings |
| Owner-facing Growth anatomy summed to USD $558 against its own USD $561 headline — **the USD $3 independent off-platform backup line was missing**, the item the internal model calls *"the cheapest insurance in the model and the item most likely to be skipped"* | MEDIUM | Row added, plus explicit `Total` rows on both explainer tables so the sum is checkable by the reader |
| **Verification over-read on revocation.** *"Only BUILD and Clerk qualify"* for a next-request bar — but Clerk's verified property is a **~60-second window**, which is a bounded window, the very thing the sentence contrasts it with | MEDIUM | Restated: **no vendor of the four clears a next-request bar; only BUILD does; Clerk comes closest at ~60 seconds**, with its configurability still `NOT VERIFIED`. **The over-read favoured a vendor, which is if anything worse than one favouring our own recommendation** |
| **The ADR template was not reconciled to this phase's own new policy** — it still mandated a header `Date:` line that P03.1 stripped from the ADRs it touched | MEDIUM | Template header updated with a `Record dates` footer, and **grandfathering stated explicitly in both `adr/README.md` and `PLANS.md`** so the fifteen untouched ADRs and five untouched plans are not swept |
| **An unfalsifiable PASS.** This very table was a placeholder reading *"recorded at commit time"* — the same governance failure `cost-model.md` §12 exists to fix | MEDIUM | Replaced with this table. **And the reviewer's inference was correct: the placeholder existed because the arithmetic had not been independently re-derived, which is why findings 1 and 2 survived to review** |
| `ADR-019`'s Option A heading still said *"no rendered map (recommended)"*, contradicting its own Decision section; break-even rates did not reproduce from the rounded savings column; *"2,500 GB served is USD $3.53"* misattributed a storage line to egress; *"45–140×"* had endpoints derived from nothing; §7's 50/300/1,200 and §1's 55/330/1,320 diverged without stating `C-A9`'s 10% re-geocode uplift | LOW | All corrected in place. |

**Findings the reviewer raised and explicitly rejected as non-issues**, recorded so they are not re-litigated: `build-vs-buy.md` §4's *"total cost of ownership, not the lowest bill"* heading (separate rows, no combined total); ratio statements such as *"human cost is 88–96% of total cost of ownership"* in `ADR-016` and `p03-decision-inputs.md` (a ratio is not a summed headline); the media comparison omitting storage cost on metered alternatives (**conservative against the selected option**); and Auth0 pricing differing between the 2026-08-11 register and the 2026-08-12 research doc (**both dated; neither claims to supersede the other**).

**Axes the review found clean:** no accidental approval anywhere and no ADR status changed to `ACCEPTED`; no dropped exclusion; rendering genuinely open in `ADR-020`; the authentication handling — *"the best work in the phase"* — with Cognito consistently recorded as *no documented mechanism* rather than *impossible* and all three residual gaps carried; the map/privacy separation; the AI wording; the language policy; and the bounded mandate, with no stack decision reopened and no P04 work started.

## Deliverables

**New:** `docs/03-technology/p03-decision-reconciliation.md` · `docs/03-technology/cost-alternatives.md` · `docs/02-architecture/adr/ADR-020-web-rendering-strategy-provisional-until-p04.md` · `docs/07-research/authentication-vendor-verification.md` · `presentation/cost-explainer-v0.1.md` · `plans/P03.1-decision-reconciliation.md`.

**Modified:** `AGENTS.md` · `PLANS.md` · `adr/ADR-013` (scope narrowed) · `adr/ADR-017` (status `HOLD`, exit argument corrected) · `adr/ADR-019` (split into three levels) · `adr/README.md` · `docs/03-technology/cost-model.md` (§1 framing, §9, §12) · `docs/03-technology/ai-evaluation.md` (§8.1) · `presentation/technology-preview-v0.1.md` · `presentation/outline.md` · `plans/README.md` · `README.md` · `CHANGELOG.md`.

## Handoff and recommended next step

**Recommended: P04 — UX Architecture. Do not execute it from this plan.**

- **P04 now has three decisions genuinely handed to it rather than pre-empted:** the web rendering strategy (`ADR-020`), whether any geographic surface is rendered (`ADR-019` Level 3), and whether AI-assisted request intake is worth testing (`ai-evaluation.md` §8.1). **P03.1 prejudges none of the three.**
- **P04 still requires its own authorization, and `G-06` is still the blocker.** `SRC-004` remains NOT RECEIVED.
- **The technology track's next action is not more evaluation.** It is David approving or rejecting the six-row packet in `p03-decision-reconciliation.md` §4, individually, with `ADR-013` carrying the most scrutiny.
- **Three cheap verifications still precede any purchase**, one now closed: the authentication one is done; the object-storage content-type seam and the two geocoding legal readings are not.

**Two things that remain free now and impossible later**, carried forward unchanged from P03: the two-column instant-plus-zone decision (`R-039`), and per-field import provenance in the first migration.

Do not treat this plan's completion as authorization for any later phase.

---

*Record dates — phase executed 2026-08-12 on branch `p03-1-decision-reconciliation`, against P03 complete at commit `3e10d14`. Authentication vendor evidence accessed 2026-08-12; all other price and terms evidence accessed 2026-08-11 and unchanged by this phase.*
